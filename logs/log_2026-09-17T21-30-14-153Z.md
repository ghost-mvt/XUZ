# Lab Log

**Timestamp:** 18‏/9‏/2026، 12:30:14 ص

---

## User Input
كود برمجي تشفير ملفات بلغة بايثون

---

## Explanation
[Code extracted below]

هذا الكود يقوم بتشفير ملف باستخدام طريقة التشفير base64. يقوم بقراءة المحتويات الأصلية للملف كبيانات ثنائية، ثم يشفرها باستخدام base64، ويحول الناتج المشفر إلى نص قابل للكتابة. ثم يقوم بكتابة النص المشفر فوق المحتويات الأصلية للملف.

هذا نموذج بسيط للتشفير، وليس آمنًا للغاية. للتشفير الآمن، يوصى باستخدام خوارزميات تشفير معيارية مثل AES مع مفتاح تشفير آمن.

---

## Code
import os
import base64

def encrypt_file(file_path):
    with open(file_path, 'rb') as file:
        original_data = file.read()
        encoded_data = base64.b64encode(original_data)
        encrypted_data = encoded_data.decode('utf-8')

    with open(file_path, 'w') as file:
        file.write(encrypted_data)

# مثال على استخدام الدالة
file_to_encrypt = 'example.txt'
encrypt_file(file_to_encrypt)
print(f"تم تشفير الملف: {file_to_encrypt}")
